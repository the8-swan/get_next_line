# include "get_next_line.h"

//tatic int pointer = 0;
/*
char *get_next_line(int fd)
{
    //char ptr[BUFFER_SIZE + 1];
    //ssize_t size;
   // size = read(fd,ptr,BUFFER_SIZE);
    //printf("%zu",size);

    return (NULL);
}
*/